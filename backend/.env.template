# MongoDB Connection (REQUIRED)
# Replace <db_password> with your actual MongoDB password
MONGODB_URI=mongodb+srv://kranvita007_db_user:<db_password>@hivefive.anukffg.mongodb.net/hivefive?retryWrites=true&w=majority&appName=HiveFive

# Firebase Admin SDK (REQUIRED - choose one option)
# Option 1: File path (if you have firebase-admin-sdk.json in backend directory)
FIREBASE_ADMIN_SDK_PATH=./firebase-admin-sdk.json

# Option 2: JSON as environment variable (uncomment and paste your Firebase Admin SDK JSON here)
# FIREBASE_ADMIN_SDK={"type":"service_account","project_id":"...",...}

# Gemini API Key (optional - for Buzz chatbot AI responses)
# Get a valid key from: https://makersuite.google.com/app/apikey (keys start with "AIza")
# GEMINI_API_KEY=your_gemini_api_key_here

# Server Port (optional, defaults to 5001)
PORT=5001

# Node Environment
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
